import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  chart: {
    width: '1000px',
    height: '500px',
    marginTop: '80px',
  },
}));

export default useStyles;
